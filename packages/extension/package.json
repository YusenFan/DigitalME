{
  "name": "@persona-engine/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Persona Engine Chrome extension — captures browsing activity and sends to daemon",
  "type": "module",
  "scripts": {
    "build": "tsup && node scripts/copy-static.js",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
