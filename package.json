{
  "name": "persona-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first system that actively builds a living behavioral model of you",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.9.0",
    "fastify": "^5.8.4",
    "vitest": "^4.1.4"
  }
}
